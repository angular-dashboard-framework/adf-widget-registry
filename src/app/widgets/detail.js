(function() {
    'use strict';

    function controller(dependencies) {
        var vm = this;

        activate();

        function activate() {

        }
    }
    angular.module('adf-widgets').controller('WidgetDetailCtrl', controller);

})();
