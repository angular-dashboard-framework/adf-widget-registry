/* global window */

(function (angular) {
    'use strict';

    angular.module('adf-widget-registry', [
      'angular-loading-bar',
      'ngAnimate',
      'ngSanitize',
      'ngResource',
      'ngRoute',
      'adf-widgets'
      ]);

})(angular);
